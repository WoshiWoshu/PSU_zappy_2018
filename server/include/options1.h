/*
** EPITECH PROJECT, 2019
** PSU_zappy_2018
** File description:
** options1
*/

#ifndef OPTIONS1_H_
#define OPTIONS1_H_

#include "server.h"

int option_p(size_t *, const char **, size_t);
int option_x(size_t *, const char **, size_t);
int option_y(size_t *, const char **, size_t);
int option_c(size_t *, const char **, size_t);
int option_f(size_t *, const char **, size_t);

#endif /* !OPTIONS1_H_ */
